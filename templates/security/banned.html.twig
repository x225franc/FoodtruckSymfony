{% extends 'base.html.twig' %}

{% block title %}Compte Banni{% endblock %}

{% block body %}
<div class="min-h-screen flex flex-col items-center justify-center">
    <h1 class="text-4xl font-bold text-gray-800 mb-4">Compte Banni</h1>
    <p class="text-gray-600 mb-8">Votre compte a été banni. Vous n'avez pas accès à cette application.</p>
    <a href="{{ path('home') }}" class="bg-green-600 hover:bg-green-700 text-white font-bold py-2 px-4 rounded transition duration-300">
        Retour à l'accueil
    </a>
</div>
{% endblock %}